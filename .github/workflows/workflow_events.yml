name: workflow events

on: 
    push:
    pull_request:
    workflow_dispatch:
    # schedule:
    #     - cron: '*/5 * * * *'

jobs: 
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo " This workflow is triggered by the ${{github.event_name }} event"
